import React from 'react';

function Home() {
  return (
    <div className="home">
      <h1>React Server Side Rendering (SSR) service</h1>
      <p>Example webapp service to render HTML dynamically in the server side with React.</p>
    </div>
  );
}

export default Home;
